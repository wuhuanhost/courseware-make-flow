<div class="reveal">
        <div class="mask"></div>
        <!-- Any section element inside of this container is displayed as a slide -->
        <div class="slides">
                <!--ppt的每一页-->
            <% for(var i=0;i<content.length;i++){ %>
                <%- content[i] %>
            <% } %>
        </div>
    </div>
</div>        